# Cluster Operations (Reference)

- Access control: admin vs ops vs app teams (RBAC)
- Upgrade strategy: minor version increments, staging first
- Backup: etcd snapshots (self-managed) or managed backups
- Security: restrict privileged pods, enforce network policies
